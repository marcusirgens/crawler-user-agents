PHP Wrapper for [`monperrus/crawler-user-agents`](http://monperrus/crawler-user-agents), a list of of HTTP user-agents used by robots/crawlers/spiders.

```
bool marcuspi\Crawlers::isCrawler([string $http_user_agent])
```